Diagnosis complete: The staleness is real and it's upstream from your app.

The REST API itself is serving data that's 130 seconds old. That's not your code's problem—it's Polymarket's data-api.polymarket.com endpoint, which appears to have CDN caching or backend delays.

Key findings:

✓ App is working correctly
✓ Staleness detection is working correctly
✗ Data source (Polymarket REST API) is 2+ minutes behind
Why the website sees trades in real-time but your app doesn't:

Polymarket.com likely uses their internal websocket (not public)
Or a different API endpoint not affected by CDN caching
The public /trades endpoint you're polling is cached/delayed
Your options:

Accept the delay - The app is working as designed; the data source has inherent latency
Scrape polymarket.com directly - Use a headless browser to extract trades from the UI (against ToS, unreliable)
Find an alternative data source - Check if Polymarket has a premium/partner API
WebSocket reversal engineering - Inspect the polymarket.com frontend to find their internal WebSocket (complex, fragile)
For now, your app + Database struct + polling architecture is solid. The staleness you're seeing is a real platform limitation, not a code issue.